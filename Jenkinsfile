pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                sh 'echo "the PWD is : ${pwd}"'
            }
        }
        // stage('start'){
        //   steps{
        //     sh 'npm start'
        //   }
        // }
    }
}